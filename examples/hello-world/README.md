# hello-world

A basic example showcasing how Inspekt works on all platforms.

Run the sample using:

* JVM: `jvmRun`
* JS: `jsNodeDevelopmentRun`
* WasmJS: `wasmJsNodeDevelopmentRun`
* Native: `runDebugExecutable<your-target-here>`